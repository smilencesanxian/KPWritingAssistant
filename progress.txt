## 2026-03-29 - 将所有AI提示词输出改为中文

### What was done:
- **修改 `src/config/prompts.json`**:
  - `correction.systemPrompt`: 添加"重要：输出语言要求"段落，明确以下字段必须使用中文：
    - `scoring_overview` 中各维度的 `comment`（评语）
    - `correction_steps` 中 step1-6 的批改详情
    - `improvement_suggestions` 中的 `title` 和 `detail`（改进建议标题和详情）
    - `highlights` 中的 `reason`（亮点分析原因）
    - `suggestion`（修改建议说明）
  - `correction.part1ExtraGuidance`: 添加中文输出要求说明
  - `correction.part2ExtraGuidance`: 添加中文输出要求说明
  - `detectType.systemPrompt`: 明确 `topic` 和 `essay_type_label` 必须使用中文

- **保留英文的字段**（按需求）：
  - `model_essay`（范文内容）- 必须用英文写作
  - `highlights.text`（原文片段）- 保留学生原文
  - `correction_steps.step4.original`（原文错误片段）- 保留学生原文
  - `error_type`（错误类型标识）- 使用英文标识如 spelling, tense 等

### Testing:
- **单元测试**: ✅ 274/274 通过
- **Lint 检查**: ✅ 通过（14 个警告均为既有问题，与本次修改无关）
- **构建验证**: ✅ `npm run build` 成功
- **JSON 验证**: ✅ `prompts.json` 格式合法

### Notes:
- 所有修改集中在 `prompts.json` 配置文件中，无需修改业务代码
- 通过在 systemPrompt 中明确标注各字段语言要求，确保AI输出符合预期
- 范文（model_essay）仍保持英文，因为PET考试要求学生用英文写作

---



### What was done:
- **BottomNav.tsx**: 将 writing-guide 导航项 label 从'导览'改为'知识库'，更新图标 SVG 为书本图标（BookOpen 风格，描边风格一致）
- **highlights.ts**: 扩展 `getCollectedSystemPhrases()`：追加查询 source='user' 且 knowledge_essay_type IS NOT NULL 的条目，与 source='system' 条目合并，返回类型改为 `Array<{ text: string; knowledge_essay_type: string | null }>`
- **model-essay/route.ts**: 在注入 Prompt 前，根据 submissionData.exam_part 过滤词句类型（part1→保留 email+general+null；part2→保留 article+story+general+null），避免类型混用
- **测试文件更新**: BottomNav.test.tsx、BottomNav.integration.test.tsx 中'导览'→'知识库'；highlights.kb5.test.ts 更新断言以匹配新返回类型

### Testing:
- 39/39 单元测试通过（BottomNav + highlights.kb5）
- npm run lint: 0 errors, 14 warnings（均为既有问题）
- npm run build: 成功

### Notes:
- getCollectedSystemPhrases() 返回类型从 string[] 改为对象数组，仅在 model-essay route 中使用，无其他调用方受影响
- 系统词句（source='system'）在 highlights_library 中 knowledge_essay_type=null，过滤时 null 条目全部通过，行为向后兼容

## 2026-03-27 - Task 34: KB-4 - 重写写作导览页面为知识库页面（三类Tab + 分类卡片 + 收藏/删除/添加）

### What was done:
- **重写 src/app/writing-guide/page.tsx：**
  - 改为 Server Component
  - 页面标题改为"写作知识库"
  - 引入并渲染 KnowledgeBaseContent 客户端组件

- **新建 KnowledgeBaseContent.tsx（客户端组件）：**
  - 管理当前 Tab 状态（email/article/story）
  - 通过 useEffect 调用 /api/recommended-phrases?essayType=xxx&grouped=true 获取数据
  - 渲染三个 Tab 切换按钮（data-testid="tab-{type}"）
  - 渲染对应 Tab 的 CategorySection 列表
  - 右下角 FAB 按钮（data-testid="add-knowledge-fab"）
  - 处理收藏、删除、添加操作，支持乐观更新
  - Toast 通知反馈操作结果

- **新建 CategorySection.tsx：**
  - 手风琴折叠/展开（默认全部展开）
  - 标题显示 category_label
  - 列表渲染 KnowledgeItem

- **新建 KnowledgeItem.tsx：**
  - 左侧显示 [基础] 灰色 Badge 或 [高级★] 橙色 Badge
  - is_in_highlights=true 时显示 🌟 金色图标
  - is_collected=true 显示蓝色 ✓ 已收藏按钮
  - 系统条目未收藏显示 收藏 按钮，点击调用 POST /api/recommended-phrases/[id]/collect
  - 用户自定义条目（source='user'）显示红色 × 按钮，点击调用 DELETE /api/highlights/[id]

- **新建 AddKnowledgeModal.tsx：**
  - 输入框（知识内容）
  - 类型选择（词汇/短语/句子）
  - 作文类型选择（邮件/文章/故事，默认当前Tab）
  - 提交调用 POST /api/highlights 传入 knowledge_essay_type

- **更新 E2E 测试文件：**
  - 更新 playwright.config.ts 使用端口 3000
  - 更新 e2e/knowledge-base.spec.ts 测试用例和 mock 数据
  - 更新 e2e/writing-guide.spec.ts 端口和选择器

### Testing:
- **E2E 测试 (knowledge-base.spec.ts)**: ✅ 8/8 通过
  - E2E-001: 页面标题显示"写作知识库"，三类 Tab 可见
  - E2E-002: Tab 切换正常
  - E2E-003: 收藏系统条目成功
  - E2E-004: 亮点库词句显示金色 🌟 标识
  - E2E-005: 手风琴折叠/展开
  - E2E-006: 添加自定义知识成功
  - E2E-007: 删除用户自定义条目
  - E2E-008: 完整用户旅程
- **E2E 测试 (writing-guide.spec.ts)**: ✅ 4/4 通过
- **代码规范**: ✅ 通过 (`npm run lint` 0 新错误)
- **构建**: ✅ 通过 (`npm run build` 成功)

### Notes:
- 保留了旧的 WritingGuideTree.tsx 文件（未删除）
- 所有新组件都添加了 data-testid 便于测试
- 端口统一为 3000（与开发服务器一致）

---

## 2026-03-27 - Task 32: KB-2 - 更新类型定义 + DB模块新增 getKnowledgeBase 函数

### What was done:
- **更新 TypeScript 类型定义 (src/types/database.ts)：**
  - `RecommendedPhrase` 新增 `category: string | null` 和 `level: 'basic' | 'advanced' | null` 字段
  - `Highlight` 新增 `knowledge_essay_type: string | null` 字段

- **新增知识库类型和常量 (src/lib/db/recommended-phrases.ts)：**
  - `KnowledgeItem` 接口：包含 id, text, type, level, category, source, is_collected, is_in_highlights
  - `KnowledgeSection` 接口：包含 category, category_label, items
  - `CATEGORY_LABELS` 常量：12个预定义分类的中文映射
  - `getCategoryLabel()` 辅助函数

- **实现 getKnowledgeBase() 函数：**
  - 采用写入时预关联方案：is_in_highlights 通过 recommended_phrase_id ID 索引查询（无 ILIKE 扫描）
  - 查询指定 essay_type 和 'general' 类型的系统推荐短语
  - 查询用户 highlights 中所有有 recommended_phrase_id 的条目，构建 Map 进行 O(1) 状态查找
  - 正确区分 is_collected（source='system'）和 is_in_highlights（source='user'）
  - 追加用户自定义条目（source='user' 且 recommended_phrase_id IS NULL）
  - 按 category 分组并附加中文标签
  - 返回按预定义顺序排列的 KnowledgeSection[] 数组

- **实现 tryLinkToKnowledgeBase() 函数 (src/lib/db/highlights.ts)：**
  - 使用 ilike 进行大小写不敏感的精确文本匹配
  - 找到匹配返回 phrase.id，未找到返回 null
  - 支持传入 supabaseClient 参数，用于写入时预关联

### Testing:
- **TypeScript 编译**: ✅ 通过 (`npm run build` 成功)
- **代码规范**: ✅ 通过 (`npm run lint` 0 新错误)
- **单元测试**: ✅ 261/261 通过
- **类型定义**: 新增字段均为可选（`| null`），保持向后兼容

### Notes:
- 性能优化：使用 Map 进行 O(1) ID 查找，避免运行时 ILIKE 扫描
- 分类顺序固定：opening → opinion → connector → detail → closing → title → plot → emotion → emotion_vocab → action_vocab → adverb → complex_sentence → other
- 用户自定义条目追加在系统条目之后，按创建时间排序

---


### What was done:
- **重写 src/app/corrections/[id]/page.tsx：**
  - 移除原有"原文批注"界面（AnnotatedEssay组件）
  - 按4大块重新组织页面结构：评分总览、批改详情、改进建议、范文示例
  - 添加 data-testid 属性便于测试定位

- **新增评分总览组件 (ScoreOverview.tsx)：**
  - 4维度表格展示：内容/沟通/组织/语言
  - 每行显示维度名、得分/5、评语
  - 底部显示总分/20
  - 向后兼容：scoring_comments为null时显示提示信息

- **新增批改详情组件 (CorrectionDetails.tsx)：**
  - 手风琴式可折叠面板，默认全部展开
  - 6个步骤各为一个折叠面板，带图标和标题
  - Step 4（语言校对）内部用表格展示[原文|问题类型|修改建议]
  - 支持"全部展开/收起"功能
  - 向后兼容：correction_steps为null时隐藏整个块

## 2026-04-07 - Task 36: FB-1 等待页面分数展示优化

### What was done:
- upload/page.tsx: 将4个维度卡的"/5"改为"满分5分"（text-xs text-neutral-400样式）
- upload/page.tsx: 删除下方分数标准表格，替换为一行说明"四个维度各 5 分，满分 20 分"

### Testing:
- npm run build: 通过
- npm run lint: 0 errors
- npx jest src/app/upload/page.test.tsx: 27/27 通过

### Notes:
- 用户反馈"/5"紧贴数字被误读为"15"，改为文字描述更清晰
- 分数标准表格无维度注明让用户困惑，删除后改为简洁总分说明

## 2026-04-07 - Task 37: FB-2 语言校对无问题时的提示文案

### What was done:
- CorrectionDetails.tsx: 将"暂无语言校对数据"改为"✓ 语言表达较好，未发现明显语法或拼写问题"（绿色text-green-600）

### Testing:
- npm run build: 通过（Compiled successfully）
- npm run lint: 0 errors

## 2026-04-07 - Task 38: FB-3 清理范文Markdown符号

### What was done:
- llm.ts: 新增 cleanMarkdown() 导出函数，在 generateModelEssay() 返回前清理 **/**/#
- ModelEssayView.tsx: displayContent 计算时对旧数据也做 Markdown 清理
- renderer.ts: wrapTextWithFontMetrics() 入口处在 split('\n') 前先清理 Markdown

### Testing:
- npm run build: 通过
- npm run lint: 0 errors
- npx jest llm.test.ts: 6/6 通过

### Notes:
- cleanMarkdown 同时处理 **bold**、*italic*、# 标题三种格式
- 历史数据通过前端展示层兜底清理，不需要数据库迁移

## 2026-04-07 - Task 39: FB-4 范文字数约束加强 + 保存报错修复

### What was done:
- prompts.json part1SystemPrompt: 追加【字数硬性限制】，100-110词，绝对不超过120词
- prompts.json part2SystemPrompt 文章类第8条: 同样加强字数约束
- prompts.json part2SystemPrompt 故事类第9条: 同样加强字数约束
- model-essays.ts: .single() 改为 .maybeSingle()，并增加空结果处理，修复保存报错

### Testing:
- npm run build: 通过
- npm run lint: 0 errors

### Notes:
- 原因: Supabase .single() 在0行时抛出 "Cannot coerce to single JSON" 错误，.maybeSingle() 返回 null 而不是报错

## 2026-04-07 - Task 40: FB-5 字帖限制为1页 + 答题纸模板确认

### What was done:
- renderer.ts: 将 pageCount 固定为1，超出内容截断（原来根据行数动态计算可多页）
- templates/index.ts: 确认代码正确（part2 → pet-part2），无需修改
- copybook route.ts: 确认 exam_part 字段正确传递，无需修改

### Testing:
- npm run build: 通过
- npx jest src/lib/pdf/: 54/54 通过

### Notes:
- 答题纸选错可能是历史数据的 exam_part 字段未设置问题，代码逻辑本身正确
- 新生成的字帖将严格限制为1页，超出的范文内容被截断
